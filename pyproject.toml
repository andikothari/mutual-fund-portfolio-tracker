[project]
name = "mutual-fund-portfolio-tracker"
version = "0.1.0"
description = ""
authors = [
    {name = "Andi Kothari",email = "andikothari@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas (>=3.0.0,<4.0.0)",
    "numpy (>=2.4.1,<3.0.0)",
    "scipy (>=1.17.0,<2.0.0)",
    "requests (>=2.32.5,<3.0.0)",
    "tabulate (>=0.9.0,<0.10.0)",
    "duckdb (>=1.4.4,<2.0.0)",
    "pyarrow (>=23.0.0,<24.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
[tool.poetry]
packages = [{ include = "src", from = "." }]
